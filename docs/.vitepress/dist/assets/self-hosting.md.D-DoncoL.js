import{c as s,Q as t,j as a,m as i}from"./chunks/framework.g_l1frA7.js";const k=JSON.parse('{"title":"Self-hosting FinSight","description":"","frontmatter":{},"headers":[],"relativePath":"self-hosting.md","filePath":"self-hosting.md","lastUpdated":1787473880000}'),n={name:"self-hosting.md"};function o(r,e,l,d,h,c){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="self-hosting-finsight" tabindex="-1">Self-hosting FinSight <a class="header-anchor" href="#self-hosting-finsight" aria-label="Permalink to &quot;Self-hosting FinSight&quot;">​</a></h1><p>FinSight&#39;s server mode (<code>finsight-server</code>) is an Immich-style self-hosted service: it runs on hardware you control, stores each user&#39;s data in a separate encrypted SQLCipher database under a single data directory, and serves the same UI you&#39;d get from the desktop app — reachable from your phone, laptop, or anyone else&#39;s device on your account, without a cloud subscription in between. This guide gets you from &quot;nothing running&quot; to a working, installable, backed-up instance.</p><p>It assumes no prior Docker or reverse-proxy experience. Read it top to bottom once; after that you&#39;ll only need the Quick Start and the recipe you picked.</p><hr><h2 id="_1-what-you-get-prerequisites" tabindex="-1">1. What you get / prerequisites <a class="header-anchor" href="#_1-what-you-get-prerequisites" aria-label="Permalink to &quot;1. What you get / prerequisites&quot;">​</a></h2><ul><li><p><strong>A Docker image, not a cloud service.</strong> You run <code>finsight-server</code> yourself, on a machine you own — a home server, a NAS, a small VPS, or even a spare laptop that stays on. Nobody else operates it or has access to your data.</p></li><li><p><strong>The server speaks plain HTTP on port <code>8674</code>.</strong> It does not terminate TLS itself. If you want HTTPS (you almost always do — browsers require it for installable PWAs and service workers), you put a reverse proxy in front of it. Section 3–5 below cover three ways to do that, from easiest to most manual.</p><p>Three client features need a <strong>secure context</strong> (HTTPS, or <code>localhost</code>) and are simply inert without one — no error, they just never happen:</p><table tabindex="0"><thead><tr><th>Feature</th><th>Without HTTPS</th></tr></thead><tbody><tr><td>Installable PWA + offline app shell</td><td>Service workers don&#39;t register</td></tr><tr><td>Encrypted offline cache</td><td><code>crypto.subtle</code> is unavailable, so the browser cache is <strong>not written at all</strong> — the app still works, it just re-fetches on every load instead of painting from cache. It never falls back to storing your financial data in the clear.</td></tr><tr><td>Share-target import, Web Push, icon badges</td><td>Depend on the service worker above</td></tr></tbody></table><p>Reaching FinSight over <code>http://&lt;lan-ip&gt;:8674</code> works, but you get a plain web app rather than an installed one. If you want the full experience, use one of the TLS recipes below.</p></li><li><p><strong>All durable state lives under one directory, mounted as <code>/data</code> inside the container.</strong> That includes <code>users.db</code> (the account registry, password verifiers, wrapped database keys, and hashed persistent sessions), <code>session.key</code> (which wraps persisted session keys), plus <code>users/&lt;uuid&gt;/data.sqlcipher</code>, backups, and import staging for each user. Unwrapped database keys exist only in server memory. Back up the whole directory; there is nothing else on the server to back up.</p></li><li><p><strong>Prerequisites on the host:</strong></p><ul><li><a href="https://docs.docker.com/engine/install/" target="_blank" rel="noreferrer">Docker Engine</a> and the <code>docker compose</code> CLI (bundled with recent Docker installs; standalone <code>docker-compose</code> also works — swap the command spelling below).</li><li>Enough disk for the image (a few hundred MB) plus your data (transaction history compresses well; budget generously if you&#39;re importing years of CSVs).</li><li>One of: a <a href="https://tailscale.com/" target="_blank" rel="noreferrer">Tailscale</a> account (free tier is fine), a domain name you control, or just your home LAN — pick whichever recipe (Section 3, 4, or 5) matches how you want to reach the server.</li></ul></li></ul><hr><h2 id="_2-quick-start-docker-compose" tabindex="-1">2. Quick start (docker-compose) <a class="header-anchor" href="#_2-quick-start-docker-compose" aria-label="Permalink to &quot;2. Quick start (docker-compose)&quot;">​</a></h2><p>From the repo root (where <code>docker-compose.yml</code> lives):</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p>The first run pulls the public multi-architecture image from GitHub Container Registry, then starts it in the background, publishing port <code>8674</code> and creating a named volume (<code>finsight-data</code>) for <code>/data</code>. AMD64 PCs and ARM64 home servers use the same Compose file. The default container has no Linux capabilities, cannot gain additional privileges, and runs with a read-only root filesystem; only <code>/data</code> and a small temporary filesystem are writable.</p><p>To build the exact checkout you cloned instead (for development or local patches), use the opt-in override:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-compose.yml</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker-compose.build.yml</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p>Watch the logs until it&#39;s ready:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> logs</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -f</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> finsight</span></span></code></pre></div><p>Then open <code>http://&lt;host&gt;:8674</code> in a browser (from the host itself, that&#39;s <code>http://localhost:8674</code>). The first visit to a fresh instance shows the <strong>setup wizard</strong>: pick an admin username and password, and the server generates a <strong>recovery key</strong>.</p><blockquote><p><strong>Save the recovery key somewhere durable — a password manager, a printed copy in a drawer — the moment it&#39;s shown.</strong> It&#39;s the only way back into an account if a password is lost; FinSight cannot reset it for you, by design (that&#39;s what makes the per-user encryption meaningful).</p></blockquote><p>The first account is the only administrator. After signing in, it can use <strong>Settings → Account → Manage users</strong> to add or delete non-admin users. Every new user receives a separate SQLCipher database and a one-time recovery key; the administrator must give that key to the user securely. Deleting a user revokes their sessions and removes their entire <code>users/&lt;uuid&gt;/</code> directory.</p><p>Passwords must contain at least 10 characters. Login and recovery share a per-username throttle: five consecutive failures trigger a 60-second cooldown, with the same behavior for unknown usernames. Sessions use a sliding 30-day lifetime and survive server restarts. Logout, recovery, account deletion, and “sign out other devices” revoke the corresponding persisted sessions.</p><p>LLM API keys and SimpleFIN access URLs are stored inside each user&#39;s encrypted database. They are not shared between users and do not depend on an OS keychain or Linux Secret Service inside the Docker container.</p><p><strong>About <code>FINSIGHT_COOKIE_SECURE</code>:</strong> <code>docker-compose.yml</code> ships with this set to <code>&quot;1&quot;</code> (secure cookies, <code>Set-Cookie: ... Secure</code>), which requires the browser to see the connection as HTTPS. That&#39;s correct once you&#39;re behind one of the reverse-proxy recipes below. If you&#39;re kicking the tyres over bare <code>http://</code> on your LAN with no proxy yet (Section 2 only, before you&#39;ve set up Section 3/4/5), the login cookie won&#39;t be accepted by the browser until you either add a proxy or temporarily set it to <code>&quot;0&quot;</code> in <code>docker-compose.yml</code>:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">environment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  FINSIGHT_COOKIE_SECURE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # bare http, no reverse proxy — LAN testing only</span></span></code></pre></div><p>Revert it to <code>&quot;1&quot;</code> once you&#39;re on HTTPS. Never leave it at <code>&quot;0&quot;</code> on anything reachable outside your own LAN.</p><p>The container sets the supported runtime variables for you:</p><table tabindex="0"><thead><tr><th>Variable</th><th style="text-align:right;">Docker default</th><th>Purpose</th></tr></thead><tbody><tr><td><code>FINSIGHT_DATA_DIR</code></td><td style="text-align:right;"><code>/data</code></td><td>Root for <code>users.db</code> and all per-user directories</td></tr><tr><td><code>FINSIGHT_UI_DIR</code></td><td style="text-align:right;"><code>/app/ui/dist</code></td><td>Built SPA/PWA assets served by the server</td></tr><tr><td><code>FINSIGHT_PORT</code></td><td style="text-align:right;"><code>8674</code></td><td>HTTP listen port</td></tr><tr><td><code>FINSIGHT_COOKIE_SECURE</code></td><td style="text-align:right;"><code>1</code></td><td>Adds the <code>Secure</code> attribute to session cookies when exactly <code>1</code></td></tr><tr><td><code>RUST_LOG</code></td><td style="text-align:right;"><code>info</code></td><td>Server log filter</td></tr></tbody></table><p>Compose also reads these operator settings from an optional <code>.env</code> file beside <code>docker-compose.yml</code> (copy <code>finsight.env.example</code> to <code>.env</code> as a starting point):</p><table tabindex="0"><thead><tr><th>Variable</th><th style="text-align:right;">Compose default</th><th>Purpose</th></tr></thead><tbody><tr><td><code>FINSIGHT_IMAGE</code></td><td style="text-align:right;"><code>ghcr.io/koushik0901/finsight:latest</code></td><td>Image tag or digest to deploy; pin a version for reproducible upgrades</td></tr><tr><td><code>FINSIGHT_HOST_PORT</code></td><td style="text-align:right;"><code>8674</code></td><td>Port exposed on the Docker host</td></tr><tr><td><code>FINSIGHT_COOKIE_SECURE</code></td><td style="text-align:right;"><code>1</code></td><td>Set to <code>0</code> only for temporary bare-HTTP LAN testing</td></tr><tr><td><code>FINSIGHT_PUBLIC_ORIGIN</code></td><td style="text-align:right;">inferred</td><td>External HTTPS origin when proxy headers are insufficient</td></tr></tbody></table><hr><h2 id="_3-recipe-a-—-tailscale-recommended" tabindex="-1">3. Recipe A — Tailscale (recommended) <a class="header-anchor" href="#_3-recipe-a-—-tailscale-recommended" aria-label="Permalink to &quot;3. Recipe A — Tailscale (recommended)&quot;">​</a></h2><p>The easiest path if you don&#39;t want to manage a domain, certificates, or port-forwarding at all. <a href="https://tailscale.com/" target="_blank" rel="noreferrer">Tailscale</a> is a mesh VPN; its <code>serve</code> feature also gets you a real, browser-trusted HTTPS certificate for free via Let&#39;s Encrypt, scoped to your own private tailnet — no public DNS record, no open port on your router.</p><ol><li><p>Install Tailscale on the host running FinSight, and on every device you&#39;ll use to access it (phone, laptop, etc.), then sign them into the same tailnet: <a href="https://tailscale.com/download" target="_blank" rel="noreferrer">https://tailscale.com/download</a>. Your own devices just sign in with your account; for another household member&#39;s phone, <a href="https://tailscale.com/docs/features/sharing/how-to/invite-any-user" target="_blank" rel="noreferrer">invite them to your tailnet</a> from the admin console&#39;s Users page — the free Personal plan currently includes six users, plenty for a household.</p></li><li><p>Enable <a href="https://tailscale.com/kb/1081/magicdns" target="_blank" rel="noreferrer">MagicDNS</a> and HTTPS certificates for your tailnet in the Tailscale admin console (Settings → enable &quot;HTTPS Certificates&quot;). If you forget, recent Tailscale versions notice during the next step and walk you through enabling both.</p></li><li><p>On the FinSight host, point Tailscale&#39;s built-in reverse proxy at the container:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tailscale</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> serve</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --bg</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8674</span></span></code></pre></div><p>This terminates TLS with a certificate Tailscale manages and issues for you, and forwards to the FinSight container over plain HTTP on the loopback interface — Docker&#39;s own port publish (<code>8674:8674</code>) is what makes port <code>8674</code> reachable here. The <code>--bg</code> flag makes the mapping persistent: it survives closing the terminal and resumes after a reboot, until you remove it with <code>tailscale serve reset</code>. Without <code>--bg</code>, serve runs in the foreground and stops on Ctrl-C. (Guides written before Tailscale 1.52 show <code>tailscale serve https / &lt;target&gt;</code> — that older syntax no longer exists; use the form above.)</p></li><li><p>Reach the server from any device on the tailnet at <code>https://&lt;device-name&gt;.&lt;tailnet-name&gt;.ts.net</code>. Keep <code>FINSIGHT_COOKIE_SECURE: &quot;1&quot;</code> (the default) — Tailscale serve is real HTTPS, so secure cookies work correctly.</p></li><li><p><code>tailscale serve status</code> shows the current mapping; <code>tailscale serve reset</code> tears it down if you want to reconfigure.</p></li></ol><p>Nothing here opens a port on your home router or exposes the server to the public internet — only devices logged into your tailnet can reach it. This is the recipe to reach for if &quot;just my household, from anywhere&quot; is the goal.</p><hr><h2 id="_4-recipe-b-—-public-domain-caddy" tabindex="-1">4. Recipe B — Public domain + Caddy <a class="header-anchor" href="#_4-recipe-b-—-public-domain-caddy" aria-label="Permalink to &quot;4. Recipe B — Public domain + Caddy&quot;">​</a></h2><p>Use this if you want FinSight reachable at a real domain name from any browser, with no VPN client required on the visiting device. This trades convenience for a materially larger attack surface: the server becomes reachable by anyone on the internet, protected only by your login. Only do this if you understand and accept that trade-off — Recipe A (Tailscale) is safer for the same &quot;access from anywhere&quot; goal.</p><p>If you go ahead: <a href="https://caddyserver.com/" target="_blank" rel="noreferrer">Caddy</a> is a reverse proxy that requests and renews Let&#39;s Encrypt certificates automatically — you don&#39;t touch certbot or manage renewal cron jobs.</p><ol><li><p>Point your domain&#39;s DNS <code>A</code>/<code>AAAA</code> record at the host&#39;s public IP, and forward ports <code>80</code> and <code>443</code> from your router to the host (Caddy needs <code>80</code> briefly for the ACME HTTP challenge, then serves on <code>443</code>).</p></li><li><p>Add a <code>Caddyfile</code> next to <code>docker-compose.yml</code>:</p><div class="language-caddyfile vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">caddyfile</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>finsight.example.com {</span></span>
<span class="line"><span>    reverse_proxy finsight:8674</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>Replace <code>finsight.example.com</code> with your real domain.</p><p>You do <strong>not</strong> need to add a <code>encode gzip zstd</code> directive: FinSight already serves brotli-precompressed assets and compresses its JSON responses itself, and Caddy passes an upstream <code>Content-Encoding</code> through untouched. Adding compression here would only make Caddy try to re-compress bytes that are already compressed.</p><p>What Caddy <em>does</em> add for free is <strong>HTTP/2</strong>. That matters here because the app is ~90 separate JavaScript chunks: over HTTP/1.1 a browser opens at most 6 connections per origin and the rest queue, while HTTP/2 multiplexes them all down one. This is the main reason to front the server with a proxy even on a LAN.</p></li><li><p>Add a <code>caddy</code> service to <code>docker-compose.yml</code> on the same Docker network as <code>finsight</code>, and drop the host port mapping on <code>finsight</code> itself (Caddy is now the only thing facing outward):</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">services</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  finsight</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ghcr.io/koushik0901/finsight:latest</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    pull_policy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">always</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    restart</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">unless-stopped</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    init</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    read_only</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    tmpfs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/tmp:size=64m</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">mode=1777</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    security_opt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">no-new-privileges:true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    cap_drop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ALL</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # no \`ports:\` here — only Caddy is exposed externally</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    volumes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">finsight-data:/data</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    environment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      FINSIGHT_COOKIE_SECURE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;1&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  caddy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">caddy:2</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    restart</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">unless-stopped</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    ports</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;80:80&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;443:443&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    volumes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">./Caddyfile:/etc/caddyfile:ro</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">caddy-data:/data</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">caddy-config:/config</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    command</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">caddy run --config /etc/caddyfile --adapter caddyfile</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">volumes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  finsight-data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  caddy-data</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  caddy-config</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span></code></pre></div></li><li><p><code>docker compose up -d</code>. Caddy fetches a certificate for your domain on first request and renews it automatically thereafter.</p></li><li><p><strong>Harden before exposing:</strong> use a strong, unique admin password (this app holds financial data); keep the host OS and Docker patched; consider fail2ban or a similar tool watching for repeated failed logins; keep <code>FINSIGHT_COOKIE_SECURE: &quot;1&quot;</code> — Caddy&#39;s HTTPS makes secure cookies work correctly, and turning it off here would send your session cookie over the internet in plaintext.</p></li></ol><hr><h2 id="_5-recipe-c-—-lan-only-mkcert" tabindex="-1">5. Recipe C — LAN only + mkcert <a class="header-anchor" href="#_5-recipe-c-—-lan-only-mkcert" aria-label="Permalink to &quot;5. Recipe C — LAN only + mkcert&quot;">​</a></h2><p>No away-from-home access, no domain, no VPN — just a trusted HTTPS certificate for devices on your home network. This is the right choice if FinSight only ever needs to be reachable from inside your house, and you&#39;d rather not touch Tailscale or the public internet at all. It still needs HTTPS, not just &quot;leave <code>FINSIGHT_COOKIE_SECURE</code> off&quot;: browsers refuse to register a PWA&#39;s service worker on a plain-<code>http://</code> origin (<code>localhost</code> is the one exception, which is why Section 2&#39;s quick test works without a proxy), so a LAN install still needs a trusted cert.</p><p><a href="https://github.com/FiloSottile/mkcert" target="_blank" rel="noreferrer"><code>mkcert</code></a> creates a local certificate authority and installs its root into your OS/browser trust store, so certs it issues are trusted without a &quot;not secure&quot; warning — but only on devices where you&#39;ve installed that root CA.</p><ol><li><p>Install <code>mkcert</code> on the FinSight host and generate a root CA plus a certificate for the host&#39;s LAN hostname or IP:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkcert</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -install</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mkcert</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> finsight.local</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 192.168.1.50</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # your host&#39;s LAN hostname / IP</span></span></code></pre></div><p>This produces <code>finsight.local+1.pem</code> (cert) and <code>finsight.local+1-key.pem</code> (key) in the current directory.</p></li><li><p>Install the mkcert root CA (<code>mkcert -CAROOT</code> shows its location) on <strong>every device</strong> that will access FinSight — phone, laptop, etc. Instructions vary by OS; mkcert&#39;s README covers Android, iOS, macOS, Windows, and Linux. This step is what makes the certificate trusted instead of just self-signed; skipping it means every browser (and the PWA install prompt) will refuse the connection.</p></li><li><p>Front the container with Caddy (or nginx) using the mkcert cert instead of Let&#39;s Encrypt — a minimal <code>Caddyfile</code>:</p><div class="language-caddyfile vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">caddyfile</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>finsight.local:443 {</span></span>
<span class="line"><span>    tls /certs/finsight.local+1.pem /certs/finsight.local+1-key.pem</span></span>
<span class="line"><span>    reverse_proxy finsight:8674</span></span>
<span class="line"><span>}</span></span></code></pre></div><p>Mount both the cert files and this <code>Caddyfile</code> into the <code>caddy</code> service from Recipe B&#39;s compose snippet (swap the <code>image: caddy:2</code> command/volumes accordingly), and give your router a static DHCP lease or local DNS entry so <code>finsight.local</code> resolves on the LAN.</p></li><li><p>Reach the server at <code>https://finsight.local</code> from any device that trusted the mkcert root. <code>FINSIGHT_COOKIE_SECURE: &quot;1&quot;</code> works normally since this is genuine (locally-trusted) HTTPS.</p></li></ol><hr><h2 id="_6-installing-the-app" tabindex="-1">6. Installing the app <a class="header-anchor" href="#_6-installing-the-app" aria-label="Permalink to &quot;6. Installing the app&quot;">​</a></h2><p>Once you&#39;re on HTTPS (any of Sections 3–5) or <code>localhost</code>, FinSight is an installable Progressive Web App — no app store, no separate binary.</p><ul><li><strong>Android / desktop Chrome (or Edge):</strong> open the site, then either use the install icon in the address bar or the browser menu → &quot;Install FinSight&quot; / &quot;Install app&quot;. It installs like a native app: its own window, its own icon in your app launcher/dock, no browser chrome.</li><li><strong>iOS Safari:</strong> open the site, tap the Share icon, then &quot;Add to Home Screen&quot;. Safari doesn&#39;t expose a separate &quot;install&quot; affordance the way Chrome does — Add to Home Screen is the equivalent, and it produces a standalone app icon that launches full-screen.</li><li><strong>Caveat — Safari&#39;s ~7-day storage eviction:</strong> iOS Safari aggressively evicts site data (including the offline IndexedDB cache) after roughly a week of the PWA not being opened. FinSight&#39;s offline cache is designed as a convenience — it shows your last-synced balances and transactions when you&#39;re offline — never as your data&#39;s source of truth. Offline boot is enabled only after a successful prior session on that device. Logout, an authentication failure, or switching users clears both the in-memory and IndexedDB caches. The server and its <code>/data</code> volume remain the source of truth.</li></ul><hr><h2 id="_7-desktop-via-pwa-no-native-binary" tabindex="-1">7. Desktop via PWA (no native binary) <a class="header-anchor" href="#_7-desktop-via-pwa-no-native-binary" aria-label="Permalink to &quot;7. Desktop via PWA (no native binary)&quot;">​</a></h2><p>FinSight has no native desktop binary — the PWA <em>is</em> the desktop app. Install it from the browser and it runs standalone with its own icon, window, and offline read cache, exactly like a native app.</p><ul><li><strong>Chrome / Edge (Windows, macOS, Linux):</strong> open the site, click the install icon in the address bar or menu → “Install FinSight” / “Install app”.</li><li><strong>macOS Safari / “Add to Dock”:</strong> open the site, File → Add to Dock (Safari 17+) or Share → Add to Dock. This gives a dock icon that launches full-screen, no browser chrome.</li><li><strong>The server URL is the app:</strong> there is no separate “Connect” screen — you navigate to <code>https://&lt;your-server&gt;</code> (Tailscale hostname, domain, or LAN address) and install from there. Subsequent launches open straight to your server.</li><li><strong>Exports</strong> (CSV/JSON) download via the browser’s normal download handling (Blob), so they land in your usual downloads location.</li><li><strong>Offline</strong> is the same as the PWA: a read-only cache of last-synced data; no additional local database beyond the browser’s IndexedDB cache.</li></ul><hr><h2 id="_8-backups-upgrades" tabindex="-1">8. Backups &amp; upgrades <a class="header-anchor" href="#_8-backups-upgrades" aria-label="Permalink to &quot;8. Backups &amp; upgrades&quot;">​</a></h2><p><strong>Per-user snapshots:</strong> Settings → Data &amp; backups creates encrypted snapshots inside that user&#39;s <code>backups/</code> directory. FinSight also creates a snapshot before applying pending database migrations and keeps the ten newest snapshots in that user&#39;s backup directory. Restoring from the UI stages a replacement database and takes a pre-restore safety snapshot. Restart the server/container to apply the staged restore before opening the account again.</p><p><strong>Whole-server disaster recovery:</strong> back up the complete <code>/data</code> volume, including <code>users.db</code> and every <code>users/&lt;uuid&gt;/</code> directory. Stop the service while copying so the SQLite database, WAL, and wrapped-key registry form one consistent snapshot:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> stop</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> finsight</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -a</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> finsight:/data/.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./finsight-data-backup</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> finsight</span></span></code></pre></div><p>To prove a backup before depending on it, copy <code>docker-compose.yml</code> and the backup into an empty test directory. Put <code>FINSIGHT_HOST_PORT=8675</code> in that directory&#39;s <code>.env</code> so it cannot collide with the live instance, then restore into the fresh Compose project and volume:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> create</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> finsight</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -a</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./finsight-data-backup/.</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> finsight:/data</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> finsight</span></span></code></pre></div><p>Do not copy a backup over a running instance. Restore into a fresh volume before starting the replacement container. Keep a copy off the Docker host, and protect it like other sensitive account data. Although financial records and integration secrets are SQLCipher-encrypted, <code>users.db</code> contains password verifiers and wrapped keys that can be attacked offline; use strong passwords and encrypt or tightly restrict the backup destination.</p><p><strong>Upgrades:</strong> pull the newer image, then recreate the container — the server takes a per-user pre-migration snapshot and then applies schema migrations automatically when that user&#39;s runtime opens. The volume itself is preserved:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pull</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p>For reproducible deployments, set <code>FINSIGHT_IMAGE</code> in a <code>.env</code> file to a published version tag (for example, <code>ghcr.io/koushik0901/finsight:0.1.0</code>) instead of the moving <code>latest</code> tag. Source-build users should pull Git changes and repeat the two-file build command from Section 2. Already-open browser tabs (including installed PWAs) detect the version mismatch via the server&#39;s <code>/api/server/about</code> handshake and show a &quot;refresh to update&quot; banner — no manual cache-busting needed.</p><p>If you used the early single-user server preview, first-run setup also recognizes a root-level <code>/data/data.sqlcipher</code> + <code>/data/db.key</code> pair. It moves the database and any WAL/SHM sidecars into the new administrator&#39;s user directory as one rollback-safe operation, then deletes the obsolete plaintext key file. An incomplete pair is treated as a migration error instead of creating an empty replacement database.</p><hr><h2 id="_9-connect-claude-or-chatgpt-mcp" tabindex="-1">9. Connect Claude or ChatGPT (MCP) <a class="header-anchor" href="#_9-connect-claude-or-chatgpt-mcp" aria-label="Permalink to &quot;9. Connect Claude or ChatGPT (MCP)&quot;">​</a></h2><p>FinSight speaks the <strong>Model Context Protocol</strong>, so you can point Claude Desktop, claude.ai, ChatGPT, or Claude Code at your server and use your own AI subscription instead of configuring a provider key for the in-app Copilot. The external assistant gets the <em>same</em> tools the in-app Copilot uses — the 43-tool set is shared code, not a reimplementation — plus five tools for reviewing and applying proposals.</p><p>Endpoint: <strong><code>https://&lt;your-server&gt;/mcp</code></strong> (shown in Settings → Connections).</p><h3 id="can-your-client-reach-it" tabindex="-1">Can your client reach it? <a class="header-anchor" href="#can-your-client-reach-it" aria-label="Permalink to &quot;Can your client reach it?&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Client</th><th>What it needs</th></tr></thead><tbody><tr><td><strong>Claude Code</strong>, <code>mcp-remote</code>, other local bridges</td><td>Any reachable URL — LAN, Tailscale, or <code>http://localhost:8674</code>. Uses an access token.</td></tr><tr><td><strong>claude.ai</strong>, <strong>Claude Desktop</strong> custom connectors, <strong>ChatGPT</strong></td><td>A <strong>publicly reachable HTTPS origin</strong>. These connect <em>from Anthropic&#39;s/OpenAI&#39;s servers</em>, not from your laptop, so <code>localhost</code> and a plain Tailscale tailnet are invisible to them. Use Recipe B (public domain + Caddy) or <code>tailscale funnel</code>. They run the OAuth flow automatically.</td></tr></tbody></table><p>Recipe A&#39;s <code>tailscale serve</code> is tailnet-only. <code>tailscale funnel --bg 8674</code> exposes the same server publicly if you&#39;d rather not run your own domain.</p><h3 id="two-ways-to-authenticate" tabindex="-1">Two ways to authenticate <a class="header-anchor" href="#two-ways-to-authenticate" aria-label="Permalink to &quot;Two ways to authenticate&quot;">​</a></h3><p><strong>Access token (for Claude Code and local bridges).</strong> Settings → Connections → Create token. The token is shown <strong>once</strong>. Then:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> mcp</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> add</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --transport</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> finsight</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://your-server/mcp</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --header</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Authorization: Bearer finsight_pat_...&quot;</span></span></code></pre></div><p><strong>OAuth (for cloud connectors).</strong> Paste <code>https://your-server/mcp</code> into the client&#39;s &quot;add custom connector&quot; box. It registers itself, sends you to FinSight&#39;s consent screen, you pick an access level, and it receives a token — no copy-pasting. The connector then appears in Settings → Connections under its own name, revocable like any other token.</p><p>OAuth-issued tokens are <strong>short-lived and renew themselves</strong>: the connector gets an access token good for an hour plus a refresh token, and swaps them silently in the background. You never see this happen. Revoking the connector in Settings kills both halves, so it cannot renew its way back in. Tokens you create by hand do <em>not</em> expire — you pasted them into a config file, and having them stop working on a timer would just look like a broken integration.</p><h3 id="widgets" tabindex="-1">Widgets <a class="header-anchor" href="#widgets" aria-label="Permalink to &quot;Widgets&quot;">​</a></h3><p>Some results come back as a small card — net worth, a spending breakdown, a transaction list, an affordability verdict — instead of a wall of JSON. This uses the MCP Apps UI standard, so a client that supports it (ChatGPT, and Claude surfaces as they adopt it) renders the card inline, and a client that doesn&#39;t just shows the same numbers as text. Nothing is lost either way; the tools are fully usable headless.</p><p>The cards are self-contained HTML with no external requests, so they work under the strictest client CSP and never phone home.</p><h3 id="access-levels" tabindex="-1">Access levels <a class="header-anchor" href="#access-levels" aria-label="Permalink to &quot;Access levels&quot;">​</a></h3><ul><li><strong>Read only</strong> — analysis only. The assistant can read accounts, transactions, budgets, goals, and every projection, but cannot change anything. Start here.</li><li><strong>Read and write</strong> — additionally lets the assistant <em>propose</em> changes and, once you agree in the conversation, apply them.</li></ul><p>With a read-write token the flow is deliberately three steps: the assistant drafts a proposal, tells you what it would change, and only calls approve + execute after you say yes. Every proposal is saved as a pending bundle visible in FinSight itself, so you can always see (and apply, or reject) what an assistant suggested.</p><p>An assistant can only apply proposals <strong>it</strong> drafted. Approving is meant to record &quot;you agreed, in this conversation&quot;, and nothing else was there to see that agreement — so a proposal made inside FinSight, or by a different connected assistant, comes back as a refusal rather than being applied. Those stay yours to review in the app. (If you have both Claude and ChatGPT connected and ask the second one to apply the first one&#39;s suggestion, this is why it declines.)</p><h3 id="security-notes" tabindex="-1">Security notes <a class="header-anchor" href="#security-notes" aria-label="Permalink to &quot;Security notes&quot;">​</a></h3><ul><li>A token is stored only as a hash, and it wraps its own copy of your database key — so a stolen <code>users.db</code> yields neither a usable token nor readable data. The flip side: <strong>the token itself unlocks your financial data</strong>, so treat it like a password.</li><li>Resetting your password with a recovery key <strong>revokes every token</strong>, including OAuth-issued connectors. Reconnect afterwards.</li><li><code>/mcp</code> never accepts session cookies, only bearer tokens.</li><li>If discovery reports the wrong origin behind your proxy (an <code>http://</code> issuer, or an internal hostname), set <strong><code>FINSIGHT_PUBLIC_ORIGIN=https://your-server</code></strong> in the container environment.</li><li>Tokens travel in an <code>Authorization</code> header. Reverse proxies don&#39;t log request headers by default; if you&#39;ve enabled that, exclude it.</li></ul><hr><h2 id="_10-current-limits" tabindex="-1">10. Current limits <a class="header-anchor" href="#_10-current-limits" aria-label="Permalink to &quot;10. Current limits&quot;">​</a></h2><ul><li><strong>Long-lived Copilot streaming requests.</strong> Chat answers stream over a single held-open HTTP request. Some reverse proxies cut idle connections after 30–60 seconds by default. Caddy&#39;s default timeouts are generous enough for this out of the box; if you&#39;re using nginx or another proxy and see Copilot answers truncate mid-stream, raise its read/proxy timeout (e.g. nginx&#39;s <code>proxy_read_timeout</code>) well above your typical answer length.</li><li><strong>No CSV share-target yet.</strong> On Android, sharing a downloaded CSV directly into the installed PWA (via the OS share sheet) isn&#39;t wired up yet — import CSVs through the in-app import flow instead. (iOS Safari doesn&#39;t support share targets for PWAs at all, so this is an Android-only gap regardless.)</li><li><strong>Offline is read-only.</strong> The offline cache lets you <em>view</em> last-synced balances, budgets, and transactions with no connection. It does not queue edits made while offline — mutations (adding a transaction, editing a budget, etc.) require connectivity and are paused, not queued, while you&#39;re offline.</li></ul>`,86)])])}const u=s(n,[["render",o]]);export{k as __pageData,u as default};
